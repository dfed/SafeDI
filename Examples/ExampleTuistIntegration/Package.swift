// swift-tools-version: 6.3
// The swift-tools-version declares the minimum version of Swift required to build this package.

import PackageDescription

// This package is here because otherwise this project's code will appear when opening SafeDI's Package.swift file.
// This package file serves no other purpose — the Tuist manifests are the
// source of truth for this example's project structure. Tuist resolves its
// SPM dependencies from `Tuist/Package.swift`, not this file.
let package = Package(name: "IgnoreMe")
