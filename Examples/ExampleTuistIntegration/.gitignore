# Tuist-generated files
*.xcodeproj/
*.xcworkspace/
Derived/
.tuist-bin/

# SPM build products and the manifest-time scan scratch directory
# (SafeDITool scan writes here during `tuist generate` — see
# Project.swift).
.build/

# Xcode user data and IDE noise
.DS_Store
xcuserdata/
*.xccheckout
*.xcscmblueprint
